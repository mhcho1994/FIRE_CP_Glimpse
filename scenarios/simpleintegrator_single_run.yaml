sim:
  experiment: single                                    # TODO: support more experiment types (e.g., parameter sweep, optimization, sensitivity analysis, etc.)
  composition: auto                                   
  backend: fmu-fmpy
  fmu_type: cs
  t0: 0.0
  tf: 5.0
  dt: 0.01
  tol: 1.0e-6

system:
  components:
    - name: simple_integrator
      model_path: models/benchmarks/SimpleIntegrator.mo

connections:


initialization:

inputs:

parameters:

disturbances:

outputs:
  log: ["u", "y",]   # TODO: input/output variables name sanity check (vs. modelDescription.xml)
  save_csv: false
  out_dir: results/runs
  run_name: simple_integrator
